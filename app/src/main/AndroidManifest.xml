<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.quizapp.android.quizzapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/quiz"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/quiz"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.quizapp.android.quizzapp.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-2747396326626657~9629522984"/>
    </application>
</manifest>